# pgadmin service routes

    location /pgadmin/ {
        proxy_pass http://pgadmin:80/;
        proxy_redirect off;
        proxy_set_header X-Script-Name /pgadmin;
        proxy_set_header X-Forwarded-Prefix /pgadmin;
        include /etc/nginx/conf.d/proxy_params.inc;
    }
